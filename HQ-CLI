# vim /etc/sysconfig/network - как я понял, тут менять не обязательно
#    username=hq-cli.au-team.irpo

hostnamectl set-hostname hq-cli.au-team.irpo
exec bash

timedatactl set-timezone Europe/Moscow

# по умолчания на порту ens18 находится dhcp, поэтому ip должен появится
# vim /etc/net/ifaces/ens18/options
#   TYPE=eth
#   BOOTPROTO=dhcp
#   DISABLE=no

# По идее маршрут по умолчанию и днс сервер передается по DHCP, но лучше наверн и прописать это
#vim /etc/net/ifaces/ens18/ipv4route
#    default via 192.168.1.65
# vim /etc/net/ifaces/ens18/resolv.conf
#     search au-team.irpo
#     nameserver 192.168.1.2

systemctl restart network
